{
  "name": "@omni-model/cloudflare",
  "version": "0.1.0",
  "description": "Cloudflare adapters for omni-model: Workers KV and Durable Object storage, plus a worker factory.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@omni-model/core": "workspace:*",
    "zod": "^4.4.0"
  }
}
