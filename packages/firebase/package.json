{
  "name": "@omni-model/firebase",
  "version": "0.1.0",
  "description": "Firebase callable-function adapter for omni-model — Auth/App Check identity, streaming chat + embeddings, Firestore storage.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@omni-model/core": "workspace:*",
    "@omni-model/storage-firestore": "workspace:*",
    "zod": "^4.4.0"
  }
}
