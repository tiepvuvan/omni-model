---
title: omni-model
description: A self-hosted, OpenAI-compatible AI proxy for your mobile and web apps.
---

omni-model is an **OpenAI-compatible AI proxy you run yourself**. Your provider API keys stay on
your infrastructure — never inside an app binary. Clients authenticate with what they already have
(Firebase App Check, Apple App Attest / DeviceCheck, Google Play Integrity, Firebase Auth,
Supabase, or any JWT), with Turnstile and reCAPTCHA Enterprise available for web/mobile
application verification. You
configure rate limits (request windows **and** token budgets) plus CEL-expression model routing
across OpenAI, Anthropic, Google Gemini, and any OpenAI-compatible endpoint.

## How to run it

<CardGroup cols={2}>
  <Card title="Docker + PostgreSQL" icon="docker" href="/installation/docker">
    One container image, one database. Run it anywhere that runs containers, and point any OpenAI
    SDK at it.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Up and running with a first request in a few minutes.
  </Card>
</CardGroup>

## How it works

```text
Client (any OpenAI SDK)
        │  { "model": "smart", "messages": [...] }
        ▼
┌─────────────────────────────────────────────┐
│  omni-model — your infrastructure           │
│   1. authenticate   App Check / App Attest / │
│                     Firebase Auth / JWT      │
│   2. rate limit     request windows +        │
│                     token budgets            │
│   3. route          CEL rules over model,    │
│                     user claims, headers     │
└───────────────┬─────────────────────────────┘
                │  translated on the fly
      ┌─────────┼───────────────┬───────────────────────┐
      ▼         ▼               ▼                       ▼
   OpenAI   Anthropic   Google Gemini   any OpenAI-compatible endpoint
```

Requests to Anthropic and Gemini are translated to and from the OpenAI wire format automatically —
streaming included — so your client keeps speaking one protocol everywhere.

## What you get

<CardGroup cols={2}>
  <Card title="Client attestation, not shared secrets" icon="shield-check" href="/security/app-attest">
    Firebase App Check, Apple App Attest & DeviceCheck, Google Play Integrity, Turnstile,
    reCAPTCHA Enterprise, Firebase Auth, Supabase, or any custom JWT. Combine app verifiers with
    <code>mode: any</code> or <code>mode: all</code>.
  </Card>
  <Card title="Rate limits that understand LLMs" icon="gauge-high" href="/reference/configuration">
    Fixed-window request limits <em>and</em> token budgets, per user / device / IP / global /
    custom expression, with conditional rules. Fail-open on storage outages.
  </Card>
  <Card title="CEL model routing" icon="route" href="/model-routing/routing">
    Map a client-facing alias like <code>"smart"</code> to a concrete provider + model by user
    tier, request shape, or headers.
  </Card>
  <Card title="Native client SDKs" icon="mobile" href="/integrations/swift">
    Drop-in usage from Swift, Kotlin, React Native, Flutter, and the web.
  </Card>
</CardGroup>

## Next steps

<CardGroup cols={2}>
  <Card title="Install with Docker" icon="docker" href="/installation/docker">
    Run the container against PostgreSQL, and make your first request.
  </Card>
  <Card title="Configuration reference" icon="book" href="/reference/configuration">
    Every environment variable, JSON block, provider, and routing option.
  </Card>
</CardGroup>
